/**
 * Notification queries — re-exported from messaging.ts for convenience.
 * All notification query logic lives in the messaging query module.
 */
export { getNotifications, getUnreadNotificationCount } from "./messaging";
